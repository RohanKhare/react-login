import React from 'react';

function Dashboard(props) {
        return(
            <div>
                <h3>Hello Welcome {props.username}</h3>
            </div>
        );
    
}
export default Dashboard;
